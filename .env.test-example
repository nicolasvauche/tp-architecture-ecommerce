# Port HTTP utilisé pendant les tests
PORT=3001

# Implémentation de persistance pour les tests
DATA_SOURCE=memory

# Base SQLite spécifique aux tests (isolée du dev)
SQLITE_DB=var/test.sqlite

# Niveau de logs (souvent réduit pour éviter le bruit)
LOG_LEVEL=silent
